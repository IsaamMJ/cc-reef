node_modules/
*.log
*.tsbuildinfo
.DS_Store
.env
.env.local
.omc/
.claude/

# dist/ intentionally tracked — Claude Code plugin users install via
# `/plugin install` which clones the repo as-is, so dist must be present
# for the plugin to run without a build step. Keep dist regenerated from
# src via `npm run build` and commit the output with each release.

